#include <iostream>
#include <string> // Más info en: http://www.cplusplus.com/reference/string/

using namespace std;

int main()
{

  // Declaración de strings

  // Concatenación o unión de strings

  // Comparación de strings

  // Largo de strings y acceso a elementos individuales (size, length, at)

  // Búsqueda y sub-cadenas (find, rfind)

  // Modificación de strings (inserción, eliminación)

  // Verifiación de cadenas vacías

  // Recuperar cadena estilo C


  /*
  string cadena = "este es un ejemplo...";
  int i = 0;
  int apariciones = 0;
  for(i = cadena.rfind("es"); i != string::npos; i = cadena.rfind("es", i))
  {
      //cout << i << endl;
      apariciones++;
      if(i == 0) break; // En caso de que el string se encuentre al comienzo
      i--;  // Se mueve a la siguiente posición para evitar encontar la misma palabra
  }
  cout << "Apariciones: " << apariciones << endl;
  */

  return 0;
}
